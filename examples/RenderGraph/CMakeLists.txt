add_executable(RenderGraph main.cpp)
target_link_libraries(RenderGraph PRIVATE ${PROJECT_NAME})
target_include_directories(RenderGraph PRIVATE ${CMAKE_SOURCE_DIR}/src)
if(TARGET Backend_D3D12)
    target_link_libraries(RenderGraph PRIVATE Backend_D3D12)
elseif(TARGET Backend_Vulkan)
    target_link_libraries(RenderGraph PRIVATE Backend_Vulkan)
elseif(TARGET Backend_Metal)
    target_link_libraries(RenderGraph PRIVATE Backend_Metal)
endif()

